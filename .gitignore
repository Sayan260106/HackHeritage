# Node / frontend
node_modules/
dist/
build/
coverage/
*.log

# Public assets & video/scrolly frame sequences
/public/
public/

# Python
__pycache__/
*.py[cod]
.venv/
venv/
env/
.pytest_cache/
.mypy_cache/

# Environment files
.env
.env.local
.env.*.local
!.env.example

# OS / editor
.DS_Store
Thumbs.db
.vscode/
.idea/

# ML-generated local artifacts
ml/data/raw/
ml/data/processed/*.csv
ml/data/processed/*.parquet
ml/evaluations/realtime_source_evidence_gate.json

# Runtime telemetry and normalized satellite snapshots — persist on deployment storage, never commit live observations
/data/realtime/*.jsonl
/data/realtime/*.json
/data/realtime/*.h5
/data/realtime/*.hdf5

# Kaggle-generated user-specific metadata / local credentials
ml/kaggle/kernel-metadata.json
**/kaggle.json

# Keep the checked-in manifest and trained model artifacts
!ml/data/processed/dataset_manifest.json
!ml/models/orca_xgb_risk.json
!ml/models/orca_xgb_risk_metadata.json
